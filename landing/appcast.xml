<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle">
    <channel>
        <title>amanu</title>
        <link>https://amanu.me/appcast.xml</link>
        <description>Updates for amanu, a local-first meeting recorder for macOS.</description>
        <language>en</language>
        <item>
            <title>0.4.12</title>
            <pubDate>Thu, 03 Sep 2026 13:12:24 +0000</pubDate>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<p>This release adds transparent, anonymous product analytics and makes the first launch from the disk image finish the installation instead of leaving Amanu on a temporary volume.</p>
<h3>What changed since v0.4.11</h3>
<ul>
<li><strong>Anonymous usage statistics are visible and optional.</strong> A final switch in first-run setup, also available on the Setup tab in Settings, controls the feature and starts on. Amanu uses a random installation UUID to count how often features are used and where workflows fail.</li>
</ul>
<ul>
<li><strong>Meeting content is never analytics data.</strong> Recordings, transcripts, summaries, calendar contents, names, paths, API keys, device names, and error text are excluded. The complete event and property list is public in the repository, and <code>amanu analytics forget</code> replaces the local UUID.</li>
</ul>
<ul>
<li><strong>Analytics stays first-party.</strong> Events go to the self-hosted Umami service at <code>stats.amanu.me</code>. Unsent events are bounded and expire after seven days; the server removes stored statistics after one year.</li>
</ul>
<ul>
<li><strong>Opening the disk image now offers to install Amanu.</strong> A copy running from the read-only image asks to move itself into Applications before registering a login item, creating the CLI link, or enabling updates. Replacing an older installation is staged so an interrupted copy cannot leave half an app.</li>
</ul>
<ul>
<li><strong>The update feed now lives with the product.</strong> New builds read the signed Sparkle appcast from <code>amanu.me</code>. A byte-identical compatibility feed remains at the old <code>samat.me</code> address so every existing installation can cross onto the new URL.</li>
</ul>
<h3>Verification</h3>
<p>The full automated suite covers the analytics queue and whitelist, the setup switch and its default, moving and replacing the application bundle, both appcast locations, and the landing-page download links. The public Umami endpoint was also exercised through HTTPS and checked against its PostgreSQL rows before the diagnostic event was removed.</p>
<h3>Requirements</h3>
<p>macOS 15 or later, on Apple Silicon or Intel. The application is signed with a Developer ID certificate, uses the hardened runtime, and the disk image carries a stapled Apple notarization ticket.</p>
<h3>Verifying the download</h3>
<p>The checksum is attached alongside the disk image.</p>
<ul>
<li><code>shasum -a 256 -c amanu-v0.4.12-macos-universal.dmg.sha256</code></li>
<li><code>spctl --assess --type open --context context:primary-signature -v amanu-v0.4.12-macos-universal.dmg</code></li>
<li><code>xcrun stapler validate amanu-v0.4.12-macos-universal.dmg</code></li>
</ul>]]></description>
            <enclosure
                url="https://github.com/gsamat/amanu/releases/download/v0.4.12/amanu-v0.4.12-macos-universal.dmg"
                sparkle:version="262"
                sparkle:shortVersionString="0.4.12"
                sparkle:edSignature="D20G7vlrpZDWdGBCycRDIOV3SfgN+k0DkvewW6D46WIB6z88QiDb/80USzv+Szoyoej38zaTrDJFrW27JaSuBA=="
                length="9324388"
                type="application/octet-stream" />
        </item>
    </channel>
</rss>
